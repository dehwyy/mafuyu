[package]
name = "mafuyu_sentry"
version = "0.1.0"
edition = "2021"

[dependencies]
sentry = { version = "0.32.2" }
sentry-tower = { version = "0.32.2", optional = true }

[features]
default = []
tower = ["dep:sentry-tower"]